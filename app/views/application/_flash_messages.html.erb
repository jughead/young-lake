<% flash.each do |msg_type, message| %>
  <div class="alert <%== flash_message_class(msg_type) %> fade in">
    <button class="close" data-dismiss="alert">&times;</button>
    <%= message %>
  </div>
<% end %>
